{
  "rules": {
    "user-color-schemes": {
      "$user_id": {
        ".write": "$user_id === auth.uid"
      }
    },
    "user-colors": {
      "$user_id": {
        ".write": "$user_id === auth.uid"
      }
    }
  }
}